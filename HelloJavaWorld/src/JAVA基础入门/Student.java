package JAVAª˘¥°»Î√≈;

import java.util.concurrent.ThreadPoolExecutor;

public class Student {
	public String id;
	public static void main(String[] args) {
		ThreadPoolExecutor thread=new ThreadPoolExecutor(0, 0, 0, null, null);
		thread.execute(null);
	}
}
